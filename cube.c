#include <stdio.h>

int main() {
    int num, i;

    printf("Enter the value of n: ");
    scanf("%d", &num);

    printf("Number   Cube\n");
    for(i = 1; i <= num4; i++) {
        printf("%d \t  %d\n", i, i*i*i);
    }

    return 0;
}